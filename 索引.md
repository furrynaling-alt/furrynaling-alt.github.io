# 纳棂项目索引 & GitHub 上传规范

---

## 一、项目文件夹功能说明

| 文件夹 / 文件 | 功能 | 能否上传 GitHub | 说明 |
|---|---|---|---|
| `.git/` | Git 本地仓库 | ❌ 不直接上传 | 这是 Git 自己的数据目录，GitHub 同步靠这个。**绝对不能删除。** |
| `.codebuddy/` | IDE 工具数据 | ❌ 不上传 | CodeBuddy 的项目缓存，与项目无关。 |
| `.uploads/` | 上传临时目录 | ❌ 不上传 | 运行时临时文件，目前为空。 |
| `node_modules/` | Node.js 依赖包 | ❌❌ **绝对不能上传** | 体积巨大（几百 MB），包含第三方代码，传上 GitHub 会被封号警告。`.gitignore` 已屏蔽。 |
| `cloudfunctions/api/` | 腾讯云函数（后端） | ⚠️ **只能传源码** | 这是部署在腾讯云开发上的后端云函数，包含用户鉴权、邮件验证码、AI 聊天等逻辑。**只能传 `*.js` 和 `package.json`**，里面的 `node_modules/` 绝对不能上传！ |
| `cloudfunctions/api/node_modules/` | 云函数的依赖包 | ❌❌ **绝对不能上传** | 已从 git 清除（273 个文件），`.gitignore` 已屏蔽。 |
| `json/` | JS 工具脚本 & JSON 配置 | ✅ 可以上传 | `token-check.js`（页面鉴权）、`deploy.js`（部署脚本）、`cloudbaserc.json`（云开发配置）等。注意：不含任何密码/token。 |
| `tpzy/` | 图片、视频、SVG 资源 | ✅ 可以上传 | 站点所有的静态资源：头像、设定图、Logo、SVG 图标等。 |
| `tdwz/` | 团队文档 | ✅ 可以上传 | 团队成员撰写的项目文档（HTML 格式），通过 `team-docs.html` 统一展示。 |
| `xmwd/` | 项目文档 | ✅ 可以上传 | 电竞平台等开发项目报告文档，通过 `doc-viewer.html` 加载展示。 |
| `CNAME` | 自定义域名 | ✅ 可以上传 | GitHub Pages 域名配置，指向你的自定义域名。 |
| `index.html` | 主页 | ✅ 可以上传 | 纳棂项目门户首页。 |
| `team.html` | 团队介绍页 | ✅ 可以上传 | HNBC 团队成员展示页。 |
| `team-docs.html` | 团队文档库 | ✅ 可以上传 | 团队文档总览列表，聚合 `tdwz/` 中的所有文档。 |
| `doc-viewer.html` | 文档查看器 | ✅ 可以上传 | 通用文档查看页面，通过 URL 参数动态加载文档内容。 |
| `updates.html` | 项目更新日志 | ✅ 可以上传 | 项目进度更新记录页面。 |
| `interact.html` | AI 互动页 | ✅ 可以上传 | 用户与 AI 互动对话页面。 |
| `.gitignore` | Git 忽略规则 | ✅ 可以上传 | 告诉 Git 哪些文件/文件夹不要跟踪上传。 |

---

## 二、上传规则总结

### ✅ 可以上传 GitHub 的：
- 所有 `.html` 静态页面
- `json/` 目录下的工具脚本和配置（不含密码/token）
- `tpzy/` 目录下的所有静态资源
- `tdwz/` 和 `xmwd/` 目录下的文档
- `cloudfunctions/api/` 下的源码 JS 文件（**不是 node_modules**）
- `.gitignore`、`CNAME`

### ❌❌ 绝对不能上传的：
- `node_modules/` — 任何位置的都别传
- SSH 私钥文件（`id_rsa`、`*.pem`、`*.key`）
- `known_hosts` 文件
- `.env` 环境变量文件
- 任何包含密码、API Key、Token 的文件
- `.codebuddy/`、`.vscode/` 等 IDE 配置

---

## 三、SSH 私钥安全 ⚠️

之前不小心把 `sshknown_hosts` 这个文件放在了项目根目录，里面包含了你的 SSH 连接信息。

- **`known_hosts`** 不是你主动生成的私钥，是 SSH 连接远程服务器后自动记录的主机指纹。虽然它不是私钥本身，但仍然暴露了你的 SSH 连接习惯和服务器地址。
- 真正的私钥在 `C:\Users\袁瑞杰\.ssh\id_rsa`（或 `id_ed25519`），**这个文件泄露了就等于你的服务器被攻破**。
- 该项目文件已被删除，`.gitignore` 里也加了屏蔽规则 `*ssh*` 和 `known_hosts*`。

### SSH 公钥/私钥检查清单：
1. 每次 `git add` 之前，确保你没有把 `C:\Users\袁瑞杰\.ssh\` 里的任何文件复制到项目目录
2. `.gitignore` 已屏蔽 `*.pem`、`*.key`、`id_rsa*`、`known_hosts*`
3. 如果你用 `git add -A` 或 `git add .`，先去 `.gitignore` 确认规则有效

---

## 四、GitHub SSH 配置引导

### 1. 生成 SSH 密钥（如果没有的话）
```bash
ssh-keygen -t ed25519 -C "你的GitHub邮箱@example.com"
# 一路回车，会在 C:\Users\袁瑞杰\.ssh\ 下生成 id_ed25519 和 id_ed25519.pub
```

### 2. 把公钥添加到 GitHub
```bash
# 复制公钥内容
cat ~/.ssh/id_ed25519.pub
```
然后去 GitHub → Settings → SSH and GPG keys → New SSH key，粘贴进去。

### 3. 测试连接
```bash
ssh -T git@github.com
# 成功会显示: Hi xxx! You've successfully authenticated...
```

### 4. 如果之前用 HTTPS 克隆的，改成 SSH：
```bash
git remote set-url origin git@github.com:你的用户名/仓库名.git
```

---

## 五、团队文档工作流

### 文档存储位置
- 团队文档（HNBC 成员报告）→ `tdwz/` 目录
- 项目文档（电竞平台等）→ `xmwd/` 目录

### 新增团队文档步骤

#### 方法一：手动创建 HTML（推荐，可控性最强）

1. 在 `tdwz/` 下创建新的 `.html` 文件，例如 `tdwz/我的项目报告.html`

2. 在你的 HTML 文件中，必须包含以下标记结构，这样 `doc-viewer.html` 才能正确解析标题和内容：
```html
<!-- 标题用 H1 -->
<h1>项目名称 - 报告标题</h1>

<!-- 元信息用这个格式 -->
<div class="doc-meta">
  <span class="doc-author">👤 作者名</span>
  <span class="doc-date">📅 2026-08-05</span>
  <span class="doc-status status-done">✅ 已完成</span>
</div>

<!-- 正文内容 -->
<article class="doc-body">
  <!-- 你的内容 -->
</article>
```

3. 在 `team-docs.html` 的文档列表数据中注册这个文件（目前是通过 JavaScript 动态加载的，需要在 JS 数组中添加条目）。

#### 方法二：让 AI 帮你生成

告诉我以下信息，我就能直接帮你生成标准格式的团队文档：
- 文档标题
- 作者名
- 日期
- 文档内容大纲/文字

#### 方法三：参考现有文档

参考 `tdwz/iOS-Dynamic-Island灵动岛.html` 的结构，这是一个完整的团队文档模板，包含：
- 面包屑导航
- 暗黑模式适配
- 响应式移动端布局
- 回到顶部按钮

### 文档如何被展示

```
用户访问 team-docs.html → 看到文档列表 → 点击某篇文档
→ 跳转到 tdwz/xxx.html 直接查看（独立页面）

或者

用户访问 doc-viewer.html?id=xxx → 通过云数据库动态加载文档内容
```

---

## 六、当前 git 状态说明

执行完整理后，需要做一次汇总提交：

```bash
git add .
git commit -m "整理项目结构 + 清除node_modules + 完善.gitignore"
git push
```

本次整理内容：
- JS/JSON 文件归入 `json/`
- 图片/SVG/视频归入 `tpzy/`
- 团队文档归入 `tdwz/`
- 项目文档目录 `xmwd/` 已创建（待填充）
- 删除垃圾文件（`server.bat`、`console.log`、SSH known_hosts）
- 从 git 历史中移除 `cloudfunctions/api/node_modules/`（273 个文件）
- 更新 `.gitignore`，屏蔽所有危险文件类型

---

## 七、注意事项

1. **永远不要用 `git add -A` 然后直接 push**，先 `git status` 看看哪些文件被加入了
2. **`cloudfunctions/api/` 只传源码**，如果以后需要更新依赖，在云开发控制台重新安装，不要传 node_modules
3. **腾讯云开发数据库** 是本项目的后端数据源，前端通过云函数 API 访问，不依赖本地 Node.js 运行
4. 本地 `node_modules/` 是给编辑器代码提示用的，生产环境不依赖它
5. 如果 GitHub 发邮件警告你传了敏感文件，立即检查并处理
